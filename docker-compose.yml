version: '3.8'

services:
  lock-server:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_PATH=/app/data/locks.db
      - ADDR=:8080
    volumes:
      - lock-data:/app/data
    restart: unless-stopped

volumes:
  lock-data:
